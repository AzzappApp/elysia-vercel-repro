// Stub entry — only purpose is to add the package to the workspace so that
// pnpm pulls Metro/Expo/RN deps into the root node_modules, mirroring azzapp.
import {isAdmin} from '@repro/shared/userHelpers';

export {isAdmin};
