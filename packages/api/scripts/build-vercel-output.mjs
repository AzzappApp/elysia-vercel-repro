#!/usr/bin/env node
// Legacy custom build script — no longer wired up to `build` in package.json.
// Kept committed to mirror azzapp/packages/api's layout.
console.log(
  'Custom build script is unused — Vercel uses the elysia framework preset.',
);
