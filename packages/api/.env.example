PORT=3000

# Drizzle / PlanetScale (fake credentials — the build does not need a working DB
# to surface the bundling issue, but the env vars must exist).
DATABASE_HOST=aws.connect.psdb.cloud
DATABASE_USERNAME=fake-user
DATABASE_PASSWORD=fake-password

SERVER_AUTH_SECRET=fake-server-secret
